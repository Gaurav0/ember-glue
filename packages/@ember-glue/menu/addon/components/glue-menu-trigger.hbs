{{#let (component this.args.triggerComponent) as |Trigger|}}
  <Trigger class="glue-menu--trigger" ...attributes>
    {{#if hasBlock}}
      {{yield @label}}
    {{else}}
      {{@label}}
    {{/if}}
  </Trigger>
{{/let}}
