{{#let (component @triggerComponent) as |Trigger|}}
  <Trigger @htmlTag="button" class="glue-menu--trigger" ...attributes>
    {{#if hasBlock}}
      {{yield @label}}
    {{else}}
      {{@label}}
    {{/if}}
  </Trigger>
{{/let}}
